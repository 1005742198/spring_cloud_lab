
version = '0.0.1-SNAPSHOT'

// exlucde eureka client starter
configurations {
    compile.exclude module: 'spring-cloud-starter-eureka'
}

dependencies {
	compile 'org.springframework.cloud:spring-cloud-starter-eureka-server'
}